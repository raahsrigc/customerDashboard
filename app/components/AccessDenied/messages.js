/*
 * AccessDenied Messages
 *
 * This contains all the text for the AccessDenied component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.AccessDenied";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the AccessDenied component!"
  }
});
