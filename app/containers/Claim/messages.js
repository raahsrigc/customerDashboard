/*
 * Claim Messages
 *
 * This contains all the text for the Claim container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.Claim";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the Claim container!"
  }
});
