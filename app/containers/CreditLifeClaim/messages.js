/*
 * CreditLifeClaim Messages
 *
 * This contains all the text for the CreditLifeClaim container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.CreditLifeClaim";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the CreditLifeClaim container!"
  }
});
