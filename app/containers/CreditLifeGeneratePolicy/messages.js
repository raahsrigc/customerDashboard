/*
 * CreditLifeGeneratePolicy Messages
 *
 * This contains all the text for the CreditLifeGeneratePolicy container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.CreditLifeGeneratePolicy";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the CreditLifeGeneratePolicy container!"
  }
});
