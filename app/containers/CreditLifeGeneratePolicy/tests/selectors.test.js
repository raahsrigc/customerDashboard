// import { selectCreditLifeGeneratePolicyDomain } from '../selectors';

describe("selectCreditLifeGeneratePolicyDomain", () => {
  it("Expect to have unit tests specified", () => {
    expect(true).toEqual(false);
  });
});
