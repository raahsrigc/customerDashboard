/*
 * CreditLifePolicy Messages
 *
 * This contains all the text for the CreditLifePolicy container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.CreditLifePolicy";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the CreditLifePolicy container!"
  }
});
