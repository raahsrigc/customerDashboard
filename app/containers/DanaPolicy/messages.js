/*
 * DanaPolicy Messages
 *
 * This contains all the text for the DanaPolicy container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.DanaPolicy";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the DanaPolicy container!"
  }
});
