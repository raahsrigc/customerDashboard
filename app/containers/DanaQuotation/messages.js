/*
 * DanaQuotation Messages
 *
 * This contains all the text for the DanaQuotation container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.DanaQuotation";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the DanaQuotation container!"
  }
});
