/*
 * DeviceBuyPolicy Messages
 *
 * This contains all the text for the DeviceBuyPolicy container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.DeviceBuyPolicy";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the DeviceBuyPolicy container!"
  }
});
