// import { selectDeviceBuyPolicyDomain } from '../selectors';

describe("selectDeviceBuyPolicyDomain", () => {
  it("Expect to have unit tests specified", () => {
    expect(true).toEqual(false);
  });
});
