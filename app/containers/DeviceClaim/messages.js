/*
 * DeviceClaim Messages
 *
 * This contains all the text for the DeviceClaim container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.DeviceClaim";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the DeviceClaim container!"
  }
});
