/*
 * DevicePolicy Messages
 *
 * This contains all the text for the DevicePolicy container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.DevicePolicy";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the DevicePolicy container!"
  }
});
