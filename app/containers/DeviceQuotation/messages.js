/*
 * DeviceQuotation Messages
 *
 * This contains all the text for the DeviceQuotation container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.DeviceQuotation";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the DeviceQuotation container!"
  }
});
