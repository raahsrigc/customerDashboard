// import { selectDeviceQuotationDomain } from '../selectors';

describe("selectDeviceQuotationDomain", () => {
  it("Expect to have unit tests specified", () => {
    expect(true).toEqual(false);
  });
});
