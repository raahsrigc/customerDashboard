/*
 * DomesticDanaPolicy Messages
 *
 * This contains all the text for the DomesticDanaPolicy container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.DomesticDanaPolicy";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the DomesticDanaPolicy container!"
  }
});
