// import { selectDomesticDanaPolicyDomain } from '../selectors';

describe("selectDomesticDanaPolicyDomain", () => {
  it("Expect to have unit tests specified", () => {
    expect(true).toEqual(false);
  });
});
