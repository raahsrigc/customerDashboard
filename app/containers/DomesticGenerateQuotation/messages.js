/*
 * DomesticGenerateQuotation Messages
 *
 * This contains all the text for the DomesticGenerateQuotation container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.DomesticGenerateQuotation";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the DomesticGenerateQuotation container!"
  }
});
