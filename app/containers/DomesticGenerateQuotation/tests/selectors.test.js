// import { selectDomesticGenerateQuotationDomain } from '../selectors';

describe("selectDomesticGenerateQuotationDomain", () => {
  it("Expect to have unit tests specified", () => {
    expect(true).toEqual(false);
  });
});
