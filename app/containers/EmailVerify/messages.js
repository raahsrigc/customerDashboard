/*
 * EmailVerify Messages
 *
 * This contains all the text for the EmailVerify container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.EmailVerify";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the EmailVerify container!"
  }
});
