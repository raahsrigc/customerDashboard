/*
 * GetStarted Messages
 *
 * This contains all the text for the GetStarted container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.GetStarted";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the GetStarted container!"
  }
});
