/*
 * InternationalGenerateQuotation Messages
 *
 * This contains all the text for the InternationalGenerateQuotation container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.InternationalGenerateQuotation";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the InternationalGenerateQuotation container!"
  }
});
