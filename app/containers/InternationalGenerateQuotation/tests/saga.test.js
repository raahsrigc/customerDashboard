/**
 * Test sagas
 */

/* eslint-disable redux-saga/yield-effects */
// import { take, call, put, select } from 'redux-saga/effects';
// import internationalGenerateQuotationSaga from '../saga';

// const generator = internationalGenerateQuotationSaga();

describe("internationalGenerateQuotationSaga Saga", () => {
  it("Expect to have unit tests specified", () => {
    expect(true).toEqual(false);
  });
});
