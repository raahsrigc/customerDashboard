// import { selectInternationalGenerateQuotationDomain } from '../selectors';

describe("selectInternationalGenerateQuotationDomain", () => {
  it("Expect to have unit tests specified", () => {
    expect(true).toEqual(false);
  });
});
