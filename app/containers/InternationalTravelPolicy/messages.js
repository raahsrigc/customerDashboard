/*
 * InternationalTravelPolicy Messages
 *
 * This contains all the text for the InternationalTravelPolicy container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.InternationalTravelPolicy";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the InternationalTravelPolicy container!"
  }
});
