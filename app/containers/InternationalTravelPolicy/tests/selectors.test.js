// import { selectInternationalTravelPolicyDomain } from '../selectors';

describe("selectInternationalTravelPolicyDomain", () => {
  it("Expect to have unit tests specified", () => {
    expect(true).toEqual(false);
  });
});
