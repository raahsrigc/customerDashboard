/*
 * MotorGenerateQuotation Messages
 *
 * This contains all the text for the MotorGenerateQuotation container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.MotorGenerateQuotation";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the MotorGenerateQuotation container!"
  }
});
