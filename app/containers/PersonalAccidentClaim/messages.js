/*
 * PersonalAccidentClaim Messages
 *
 * This contains all the text for the PersonalAccidentClaim container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.PersonalAccidentClaim";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the PersonalAccidentClaim container!"
  }
});
