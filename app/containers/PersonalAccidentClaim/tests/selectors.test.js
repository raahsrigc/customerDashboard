// import { selectPersonalAccidentClaimDomain } from '../selectors';

describe("selectPersonalAccidentClaimDomain", () => {
  it("Expect to have unit tests specified", () => {
    expect(true).toEqual(false);
  });
});
