/*
 * PersonalAccidentGenerateQuotation Messages
 *
 * This contains all the text for the PersonalAccidentGenerateQuotation container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.PersonalAccidentGenerateQuotation";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the PersonalAccidentGenerateQuotation container!"
  }
});
