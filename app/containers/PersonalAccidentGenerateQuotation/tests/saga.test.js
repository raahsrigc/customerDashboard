/**
 * Test sagas
 */

/* eslint-disable redux-saga/yield-effects */
// import { take, call, put, select } from 'redux-saga/effects';
// import personalAccidentGenerateQuotationSaga from '../saga';

// const generator = personalAccidentGenerateQuotationSaga();

describe("personalAccidentGenerateQuotationSaga Saga", () => {
  it("Expect to have unit tests specified", () => {
    expect(true).toEqual(false);
  });
});
