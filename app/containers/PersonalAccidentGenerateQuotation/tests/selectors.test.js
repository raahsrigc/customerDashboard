// import { selectPersonalAccidentGenerateQuotationDomain } from '../selectors';

describe("selectPersonalAccidentGenerateQuotationDomain", () => {
  it("Expect to have unit tests specified", () => {
    expect(true).toEqual(false);
  });
});
