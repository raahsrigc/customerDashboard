/*
 * PersonalAccidentPolicy Messages
 *
 * This contains all the text for the PersonalAccidentPolicy container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.PersonalAccidentPolicy";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the PersonalAccidentPolicy container!"
  }
});
