// import { selectPersonalAccidentPolicyDomain } from '../selectors';

describe("selectPersonalAccidentPolicyDomain", () => {
  it("Expect to have unit tests specified", () => {
    expect(true).toEqual(false);
  });
});
