/*
 * PersonalAccidentQuotation Messages
 *
 * This contains all the text for the PersonalAccidentQuotation container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.PersonalAccidentQuotation";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the PersonalAccidentQuotation container!"
  }
});
