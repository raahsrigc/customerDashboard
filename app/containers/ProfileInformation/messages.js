/*
 * ProfileInformation Messages
 *
 * This contains all the text for the ProfileInformation container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.ProfileInformation";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the ProfileInformation container!"
  }
});
