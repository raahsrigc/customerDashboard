/*
 * Quotation Messages
 *
 * This contains all the text for the Quotation container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.Quotation";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the Quotation container!"
  }
});
