// import { selectSessionExpiredDomain } from '../selectors';

describe("selectSessionExpiredDomain", () => {
  it("Expect to have unit tests specified", () => {
    expect(true).toEqual(false);
  });
});
