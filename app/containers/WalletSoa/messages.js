/*
 * WalletSoa Messages
 *
 * This contains all the text for the WalletSoa container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.WalletSoa";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the WalletSoa container!"
  }
});
